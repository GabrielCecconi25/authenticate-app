#file: noinspection SpellCheckingInspection
version: '3.8'

services:
  db:
    image: postgres:17
    container_name: database-auth
    restart: always
    env_file:
      - ../src/main/resources/.env
    ports:
      - "5432:5432"
