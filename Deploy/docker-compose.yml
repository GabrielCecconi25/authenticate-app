#file: noinspection SpellCheckingInspection
services:
  db:
    image: postgres:17
    container_name: database-auth
    restart: always
    env_file:
      - ../src/main/resources/.env
    ports:
      - "5432:5432"
    volumes:
      - ./scriptTables.sql:/docker-entrypoint-initdb.d/tables.sql
